const ReservationDrawer = () => {\n  // Assuming we have state variables for the required data\n  const [orderId, setOrderId] = useState('');\n  const [paymentInfo, setPaymentInfo] = useState({});\n  const [payload, setPayload] = useState({\n    fullName: '',\n    email: '',\n    phone: '',\n    quantity: 0,\n    city: '',\n    orderId: '',\n    total: 0\n  });\n  \n  const handleReservation = (data) => {\n    // Function to handle reservation logic\n    setPayload({\n      fullName: data.fullName,\n      email: data.email,\n      phone: data.phone,\n      quantity: data.quantity,\n      city: data.city,\n      orderId: data.orderId,\n      total: data.total\n    });\n    setOrderId(data.orderId);\n    setPaymentInfo(data.paymentInfo);\n  };\n  \n  return (\n    <div>\n      <h1>Reservation Successful</h1>\n      {orderId && <p>Your Order ID: {orderId}</p>}\n      {paymentInfo && <p>Payment Info: {JSON.stringify(paymentInfo)}</p>}\n    </div>\n  );\n};\n\nexport default ReservationDrawer;